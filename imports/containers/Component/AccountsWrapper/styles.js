const styles = theme => ({
  group: {
    flexDirection: "row",
    justifyContent: "center"
  },

  formControl: {
    marginBottom: "30px"
  },

  formControlButtom: {
    width: "100%",
    marginBottom: "100px"
  },

  formToggle: {
    height: "42px",
    border: "none",
    cursor: "pointer",
    color: "red"
  }
});

export default styles;
