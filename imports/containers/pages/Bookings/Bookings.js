import React from "react";
import styles from "./styles";
import { withStyles } from "@material-ui/core/styles";

const Bookings = ({ classes }) => {
  return <p>This is the Bookings page.</p>;
};

export default withStyles(styles)(Bookings);
