const styles = theme => ({
  root: {
    textAlign: "center"
  },

  logo: {
    display: "flex",
    justifyContent: "center"
  },

  loginWrapper: {
    margin: "0 30%"
  },

  formControl: {
    marginButton: "40px"
  },

  test: {
    margin: "40px",
    width: "100%"
  }
});

export default styles;
