const styles = theme => ({
  root: {
    textAlign: "center"
  },

  banner: {
    display: "flex",
    flexDirection: "row"
  },

  loginWrapper: {
    margin: "0 30%"
  },

  formControl: {
    marginButton: "40px"
  },

  test: {
    margin: "40px",
    width: "100%"
  }
});

export default styles;
